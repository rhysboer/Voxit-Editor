#include "Settings.h"

namespace Settings {
	glm::vec3 backgroundColour = glm::vec3(0.25f, 0.25f, 0.25f);
	glm::vec3 sunDirection = glm::vec3(0.58f, 0.58f, 0.58f);

	bool useVoxelOutline = true;
	bool useShadow = true;
	bool useGrid = true;
	bool useLighting = true;
	bool useHighlighterSize = true;
}